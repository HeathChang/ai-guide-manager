export const harnessVisionTemplate = `---
title: Vision — 최종 비전 (유저 작성)
category: 하네스
---

# Vision

> 이 파일은 **유저가 작성하고 갱신한다**.
> 모든 에이전트(Planner, Researcher, Implementer, Reviewer, QA, Security Auditor, Guardian, Reporter)는
> 이 문서를 **유일한 진실의 원천(Single Source of Truth)** 으로 삼는다.
>
> 빈칸을 채우고 필요하면 섹션을 확장하되, 구조는 유지한다.

---

## 1. 한 줄 목표

> 이 프로젝트가 존재해야 하는 이유를 한 문장으로.
> 기술 용어 없이. 제품 관점에서.

**예시:**
- \`중소 카페 사장이 월 5분 이내로 재고를 관리하고 자동 발주를 받을 수 있게 한다.\`
- \`사내 온보딩 문서를 검색 한 번으로 찾을 수 있게 한다.\`

**작성:**
\`\`\`
(여기에 한 줄 작성)
\`\`\`

---

## 2. 사용자와 가치

### 2.1 Primary User

- **누구:** (예: 카페 사장, 주 근무 10시간 이상)
- **현재 어떻게 해결 중:** (예: 수기 장부 + 엑셀)
- **현재 방식의 고통:** (예: 월말 재고 정산에 4시간 소요, 누락 잦음)

### 2.2 얻는 가치

유저가 이 프로덕트를 쓰고 난 후 말할 수 있어야 하는 한 문장.

\`\`\`
(작성)
\`\`\`

---

## 3. 성공 기준 (Definition of Done for the Vision)

숫자로 측정 가능한 기준 2~4개. "좋은 UX" 같은 추상 표현 금지.

- [ ] (예: 재고 등록부터 발주까지 5분 이내)
- [ ] (예: 주간 활성 사용자 100명 달성)
- [ ] (예: 오류율 1% 미만)

---

## 4. In Scope (해야 하는 것)

해결 범위 안의 기능을 불릿으로 나열.

-
-
-

---

## 5. Out of Scope (하지 않는 것)

**이게 가장 중요하다.** 에이전트는 여기에 적힌 항목을 구현 시도하면 Guardian이 VIOLATION으로 차단한다.

-
-

**예시:**
- \`다국어 지원 (v1에서는 한국어만)\`
- \`모바일 네이티브 앱 (웹 반응형까지만)\`
- \`실시간 알림 (주간 요약 이메일까지만)\`

---

## 6. 기술적 제약 (Hard Constraints)

바꿀 수 없는 사실들. 이 제약 위에서 설계해야 한다.

| 항목 | 값 | 비고 |
|------|-----|------|
| 언어/프레임워크 | (예: TypeScript, React 18) | |
| 배포 환경 | (예: Vercel, AWS Lambda) | |
| 데이터베이스 | (예: Postgres 15) | |
| 외부 의존성 | (예: Stripe, SendGrid) | |
| 성능 SLA | (예: P95 응답 < 500ms) | |
| 보안 요구사항 | (예: PII 암호화, 감사 로그) | |

---

## 7. 비기능 요구사항 (Non-Functional)

- **성능:**
- **보안:**
- **접근성:**
- **운영:** (로그, 모니터링, 알림)

---

## 8. 용어집 (Glossary)

에이전트들이 같은 용어를 같은 뜻으로 쓰도록.

| 용어 | 정의 |
|------|------|
| | |
| | |

---

## 9. 열린 질문 (Open Questions)

에이전트가 이 섹션을 보고 Researcher에게 조사를 요청한다.

- [ ]
- [ ]

---

## 10. 변경 이력

| 날짜 | 변경 | 사유 |
|------|------|------|
| YYYY-MM-DD | 최초 작성 | — |

---

## AI 에이전트에게

- 이 문서가 **유일한 진실의 원천**이다.
- 여기 적히지 않은 결정은 유저에게 **반드시 질문**한다.
- 5번(Out of Scope)은 VIOLATION 차단의 근거가 된다.
- 이 문서를 네가 **수정해야 한다고 판단**되면 Guardian을 통해 유저에게 승인 요청한다. 직접 편집 금지.

---

## 📝 채워진 예시 (참고용 — 실제 작성 시 삭제하고 위 섹션만 남긴다)

<details>
<summary>예시: 중소 카페 재고 관리 SaaS</summary>

### 1. 한 줄 목표
\`중소 카페 사장이 월 5분 이내로 재고를 관리하고 임박 소진 항목에 자동 발주 링크를 받을 수 있게 한다.\`

### 2. 사용자와 가치
- **Primary:** 개인 운영 카페 사장 (1인~3인), 재고를 엑셀/수기로 관리 중, 월말 정산에 3~4시간 소요
- **가치:** "재고 정산이 사라졌고 품절로 손님 놓치는 일이 없어졌다"

### 3. 성공 기준
- [ ] 재고 등록부터 첫 자동 발주 트리거까지 셋업 **5분 이내**
- [ ] 출시 3개월 주간 활성 사용자 **50곳 이상**
- [ ] P95 API 응답시간 **300ms 이하**
- [ ] 자동 발주 추천 정확도(사용자 수용률) **70% 이상**

### 4. In Scope
- 재고 아이템 CRUD
- 일별 사용량 기록 (수동 입력)
- 소진 예측 기반 자동 발주 추천
- 발주처 링크 푸시(웹 알림, 이메일)
- 단일 매장 관리

### 5. Out of Scope
- 멀티 매장·프랜차이즈
- POS 연동 (v2)
- 결제·정산
- 모바일 네이티브 앱 (반응형까지만)
- 다국어 (한국어만)

### 6. 기술 제약
| 항목 | 값 |
|------|-----|
| 언어 | TypeScript |
| 프론트 | React 18 + Vite |
| 백엔드 | Node.js 20 + Fastify |
| DB | Postgres 15 |
| 배포 | Vercel(프론트) + Fly.io(API) |
| 성능 SLA | P95 < 300ms |
| 보안 | 비밀번호 argon2, 감사 로그, HTTPS |

### 7. 비기능
- **성능:** 재고 리스트 로딩 < 500ms on 3G
- **보안:** PII는 이메일·전화번호만, 결제 정보 수집 안 함
- **접근성:** WCAG 2.1 AA
- **운영:** 에러 Sentry, 주요 이벤트 Axiom 로깅

### 8. 용어집
| 용어 | 정의 |
|------|------|
| 재고 아이템 | 관리 대상 개별 물품 (예: 원두 200g 봉지) |
| 소진 임박 | 예측된 7일 내 재고 고갈 상태 |
| 자동 발주 링크 | 발주처 주문 페이지로 향하는 개인화 URL |

### 9. 열린 질문
- [ ] 발주처 API가 없는 공급사는 어떻게 처리? (링크만? 복사 가능한 메시지?)
- [ ] 재고 단위 정규화를 어디까지? (g, kg, 개)

</details>
`;
